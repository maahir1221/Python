from UserDefineModule.Demo2.DataType import *
from UserDefineModule.Demo2.Logic import *


# I created a design the Logic file and a list and Tuple in DataType file
# After creation, I imported them to the F file where the syntax is:
# from directory import *
# here * means it will import every variable and data from the respective file
# if instead of importing all any specific file is required, than we can just type variable instead of *
# The main purpose to import both file to F is:
# 1. I can run the files simultaneously by running F file itself
# 2. It is not required to run them individually